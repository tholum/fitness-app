# Auto-install peer deps (React 19 / Next 15 ergonomics).
auto-install-peers=true
# NOTE: the supply-chain cooldown (minimumReleaseAge) lives in
# pnpm-workspace.yaml — that's where pnpm >= 10.16 reads it.
